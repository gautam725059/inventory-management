node_modules/
.next/
out/
*.log
.DS_Store

# Local env (admin password, etc.)
.env*.local

# Local inventory data (uncomment to keep data out of git)
# data/items.json

# Full production store dumps (users, passwordHashes, session tokens) — never commit.
import-backup-*.json
